This directory is intentionally not a valid signature bundle.

Use scripts/import-clamav-signatures.ps1 with an approved external directory
containing SHA256SUMS, APPROVED-OFFLINE-BUNDLE and one or more official
main/daily/bytecode .cvd or .cld files. No network is attached to the importer.
